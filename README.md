# yastation - CLI to control yandex speakers

## Usage

You must install all dependencies using `pip install -r requirements.txt` and provide `config.yaml`:
```yaml
username: "<your yandex username>"
password: "<your yandex password>"
```

